# Game inventory (generated)

> **Total titles:** 237
> Generated by `web/scripts/catalog-game-inventory.mjs`. Do not hand-edit; regenerate after catalog changes.

## Summary

- **Core:** 99
- **Seed-only:** 138

## Per-title registry

| # | id | name | category | source | inferred engine |
|---|-----|------|----------|--------|-----------------|
| 1 | `algebra_blaster` | Algebra Blaster | StormBattle | core | `math` |
| 2 | `algebra-attack` | Algebra Attack | StormEduPlus | seed | `math` |
| 3 | `algebra-dash` | Geometry Dash Edu | StormEduPlus | seed | `math` |
| 4 | `algo-quest` | Algo Quest | StormEduPlus | seed | `quiz` |
| 5 | `alien-blitz` | Alien Blitz | StormNeon | core | `space` |
| 6 | `anagram-storm` | Anagram Storm | StormNeon | core | `scramble` |
| 7 | `ancient-quest` | Ancient Quest | StormEduPlus | seed | `quiz` |
| 8 | `arena-legends` | Arena Legends | StormElite | seed | `placeholder` |
| 9 | `arkanoid-x` | Arkanoid X | StormNeon | core | `breakout` |
| 10 | `asteroid-storm` | Asteroid Storm | StormNeon | core | `asteroids` |
| 11 | `asteroids-blast` | Asteroids Blast | StormRetro | seed | `asteroids` |
| 12 | `astromath_wars` | AstroMath Wars | StormBattle | core | `math` |
| 13 | `atom-smash` | Atom Smash | StormEduPlus | seed | `quiz` |
| 14 | `battle-ships-x` | Battle Ships X | StormElite | seed | `placeholder` |
| 15 | `battlezone-x` | Battlezone X | StormElite | seed | `geometry` |
| 16 | `beat-blitz` | Beat Blitz | StormElite | seed | `placeholder` |
| 17 | `binary-blast` | Binary Blast | StormEduPlus | seed | `math` |
| 18 | `bio-burst` | Bio Burst | StormEduPlus | seed | `quiz` |
| 19 | `block-storm` | Block Storm | StormNeon | core | `tetris` |
| 20 | `brain_boost` | Brain Boost | StormQuick | core | `geometry` |
| 21 | `brain-blitz` | Brain Blitz | StormNeon | core | `quiz` |
| 22 | `breakout-blitz` | Breakout Blitz | StormRetro | seed | `breakout` |
| 23 | `brick-crusher` | Brick Crusher | StormNeon | core | `breakout` |
| 24 | `brick-storm` | Brick Storm | StormRetro | seed | `breakout` |
| 25 | `bull_run_logic` | Bull Run Logic | StormDash | core | `geometry` |
| 26 | `bullet-hell-x` | Bullet Hell X | StormElite | seed | `space` |
| 27 | `burger-time-x` | Burger Time X | StormRetro | seed | `maze` |
| 28 | `capital-clash` | Capital Clash | StormEduPlus | seed | `quiz` |
| 29 | `castle-siege` | Castle Siege | StormMario | seed | `geometry` |
| 30 | `centipede-rush` | Centipede Rush | StormRetro | seed | `space` |
| 31 | `chem_jump` | Chem Jump | StormDash | core | `geometry` |
| 32 | `chem_lab_chaos` | Chem Lab Chaos | StormBattle | core | `geometry` |
| 33 | `chemistry-quest` | Chemistry Quest | StormEduPlus | seed | `quiz` |
| 34 | `chess-storm` | Chess Storm | StormElite | seed | `placeholder` |
| 35 | `civics-storm` | Civics Storm | StormEduPlus | seed | `quiz` |
| 36 | `code_breaker` | Code Breaker | StormPuzzle | core | `breakout` |
| 37 | `code-logic` | Code Logic | StormEduPlus | seed | `quiz` |
| 38 | `code-quest` | Code Quest | StormNeon | core | `quiz` |
| 39 | `coin-blitz` | Coin Blitz | StormMario | seed | `geometry` |
| 40 | `color_equation` | Color Equation | StormQuick | core | `geometry` |
| 41 | `color-match` | Color Match | StormRetro | seed | `tetris` |
| 42 | `color-rush` | Color Rush | StormElite | seed | `quiz` |
| 43 | `context_clue_hunt` | Context Clue Hunt | StormPuzzle | core | `geometry` |
| 44 | `coordinate_conquest` | Coordinate Conquest | StormBattle | core | `geometry` |
| 45 | `cosmic-blast` | Cosmic Blast | StormNeon | core | `asteroids` |
| 46 | `crypt-runner` | Crypt Runner | StormElite | seed | `maze` |
| 47 | `crystal-castles-x` | Crystal Castles X | StormRetro | seed | `maze` |
| 48 | `cyber-highway` | Cyber Highway | StormElite | seed | `geometry` |
| 49 | `cyber-ninja` | Cyber Ninja | StormElite | seed | `geometry` |
| 50 | `cyber-soccer` | Cyber Soccer | StormElite | seed | `placeholder` |
| 51 | `cyber-sprint` | Cyber Sprint | StormElite | seed | `geometry` |
| 52 | `data_defender` | Data Defender | StormBattle | core | `geometry` |
| 53 | `debate_dash` | Debate Dash | StormDash | core | `geometry` |
| 54 | `debug-dash` | Debug Dash | StormEduPlus | seed | `quiz` |
| 55 | `defender-wave` | Defender Wave | StormElite | seed | `space` |
| 56 | `desert-rally` | Desert Rally X | StormElite | seed | `geometry` |
| 57 | `dig-dug-x` | Dig Dug X | StormRetro | seed | `maze` |
| 58 | `donkey-run` | Donkey Run | StormRetro | seed | `geometry` |
| 59 | `drift-king-x` | Drift King X | StormElite | seed | `geometry` |
| 60 | `dual-storm` | Dual Storm | StormElite | seed | `space` |
| 61 | `dungeon-crawl` | Dungeon Crawl | StormElite | seed | `maze` |
| 62 | `dungeon-crawler` | Dungeon Crawler | StormElite | seed | `maze` |
| 63 | `dungeon-run` | Dungeon Run | StormRetro | seed | `maze` |
| 64 | `dungeon-storm` | Dungeon Storm | StormElite | seed | `maze` |
| 65 | `equation_escape` | Equation Escape | StormDash | core | `geometry` |
| 66 | `essay_builder_rush` | Essay Builder Rush | StormDash | core | `geometry` |
| 67 | `financial_literacy_run` | Financial Literacy Run | StormBattle | core | `geometry` |
| 68 | `flag-frenzy` | Flag Frenzy | StormEduPlus | seed | `quiz` |
| 69 | `flappy-storm` | Flappy Storm | StormRetro | seed | `geometry` |
| 70 | `flash_fact_frenzy` | Flash Fact Frenzy | StormQuick | core | `geometry` |
| 71 | `fraction_frenzy` | Fraction Frenzy | StormBattle | core | `math` |
| 72 | `fraction-fighter` | Fraction Fighter | StormNeon | core | `math` |
| 73 | `fraction-frenzy` | Fraction Frenzy | StormEduPlus | seed | `math` |
| 74 | `frogger-rush` | Frogger Rush | StormRetro | seed | `maze` |
| 75 | `galaga-storm` | Galaga Storm | StormRetro | seed | `space` |
| 76 | `galaxian-x` | Galaxian X | StormRetro | seed | `space` |
| 77 | `galaxy-storm` | Galaxy Storm | StormNeon | core | `space` |
| 78 | `geography-hero` | Geography Hero | StormEduPlus | seed | `quiz` |
| 79 | `geography-quest` | Geography Quest | StormNeon | core | `quiz` |
| 80 | `geometry_defender` | Geometry Defender | StormBattle | core | `quiz` |
| 81 | `geometry_glide` | Geometry Glide | StormDash | core | `quiz` |
| 82 | `geometry_runner_3d` | Geometry Runner 3D | Storm3D | core | `quiz` |
| 83 | `geometry-dash-storm` | Geometry Dash Storm | StormElite | seed | `geometry` |
| 84 | `grammar_clicker` | Grammar Clicker | StormQuick | core | `quiz` |
| 85 | `grammar_gladiator` | Grammar Gladiator | StormBattle | core | `quiz` |
| 86 | `grammar-quest` | Grammar Quest | StormEduPlus | seed | `typing` |
| 87 | `gravity-flip-x` | Gravity Flip X | StormElite | seed | `geometry` |
| 88 | `gravity-well` | Gravity Well | StormRetro | seed | `geometry` |
| 89 | `grid-master` | Grid Master | StormNeon | core | `tetris` |
| 90 | `history_dash` | History Dash | StormDash | core | `quiz` |
| 91 | `history_timeline_rush` | History Timeline Rush | StormBattle | core | `quiz` |
| 92 | `history-blast` | History Blast | StormNeon | core | `quiz` |
| 93 | `history-blitz` | History Blitz | StormEduPlus | seed | `quiz` |
| 94 | `horde-rush` | Horde Rush | StormElite | seed | `space` |
| 95 | `ice-hockey-x` | Ice Hockey X | StormElite | seed | `placeholder` |
| 96 | `joust-fighter` | Joust Fighter | StormElite | seed | `placeholder` |
| 97 | `joust-storm` | Joust Storm | StormRetro | seed | `maze` |
| 98 | `kart-storm` | Kart Storm | StormMario | seed | `geometry` |
| 99 | `logic_tower` | Logic Tower | StormPuzzle | core | `td` |
| 100 | `logic_tunnel` | Logic Tunnel | StormDash | core | `geometry` |
| 101 | `logic-blast` | Logic Blast | StormRetro | seed | `quiz` |
| 102 | `loop-storm` | Loop Storm | StormEduPlus | seed | `math` |
| 103 | `lunar-lander-x` | Lunar Lander X | StormRetro | seed | `maze` |
| 104 | `map-master` | Map Master | StormEduPlus | seed | `quiz` |
| 105 | `market_mayhem` | Market Mayhem | StormDash | core | `geometry` |
| 106 | `math_galaxy_3d` | Math Galaxy 3D | Storm3D | core | `math` |
| 107 | `math-elite` | Math Elite | StormNeon | core | `math` |
| 108 | `math-quest` | Math Quest | StormEduPlus | seed | `math` |
| 109 | `math-storm` | Math Storm | StormNeon | core | `math` |
| 110 | `maze_of_ratios` | Maze of Ratios | StormDash | core | `maze` |
| 111 | `maze-escape-x` | Maze Escape X | StormRetro | seed | `maze` |
| 112 | `maze-runner-x` | Maze Runner X | StormRetro | seed | `maze` |
| 113 | `memory_matrix` | Memory Matrix | StormQuick | core | `memory` |
| 114 | `memory-flash` | Memory Flash | StormRetro | seed | `memory` |
| 115 | `memory-matrix` | Memory Matrix (Neon) | StormNeon | core | `memory` |
| 116 | `missile-command-x` | Missile Command X | StormElite | seed | `space` |
| 117 | `moon-patrol-x` | Moon Patrol X | StormElite | seed | `maze` |
| 118 | `mr-do-x` | Mr Do X | StormRetro | seed | `maze` |
| 119 | `multiplication_meteors` | Multiplication Meteors | StormBattle | core | `geometry` |
| 120 | `mushroom-dash` | Mushroom Dash | StormMario | seed | `geometry` |
| 121 | `neon-breakout` | Neon Breakout | StormNeon | core | `breakout` |
| 122 | `neon-chess` | Neon Chess | StormElite | seed | `placeholder` |
| 123 | `neon-circuit-racer` | Neon Circuit Racer | StormElite | seed | `geometry` |
| 124 | `neon-dungeon` | Neon Dungeon | StormElite | seed | `maze` |
| 125 | `neon-fortress` | Neon Fortress | StormElite | seed | `td` |
| 126 | `neon-jump` | Neon Jump | StormNeon | core | `geometry` |
| 127 | `neon-jumper` | Neon Jumper | StormElite | seed | `geometry` |
| 128 | `neon-kombat` | Neon Kombat | StormElite | seed | `placeholder` |
| 129 | `neon-maze` | Neon Maze | StormRetro | seed | `maze` |
| 130 | `neon-pong` | Neon Pong | StormRetro | seed | `breakout` |
| 131 | `neon-racer` | Neon Racer | StormElite | seed | `geometry` |
| 132 | `neon-viper` | Neon Viper | StormNeon | core | `snake` |
| 133 | `neon-viper-xl` | Neon Viper XL | StormNeon | core | `snake` |
| 134 | `number_catch` | Number Catch | StormQuick | core | `math` |
| 135 | `number-battle` | Number Battle | StormNeon | core | `math` |
| 136 | `number-ninja` | Number Ninja | StormEduPlus | seed | `math` |
| 137 | `number-storm` | Number Storm | StormRetro | seed | `math` |
| 138 | `pac-maze` | Pac Maze | StormRetro | seed | `maze` |
| 139 | `pattern_blast` | Pattern Blast | StormPuzzle | core | `space` |
| 140 | `pattern-match` | Pattern Match | StormRetro | seed | `memory` |
| 141 | `pattern-storm` | Pattern Storm | StormNeon | core | `quiz` |
| 142 | `phoenix-x` | Phoenix X | StormRetro | seed | `space` |
| 143 | `physics_platform` | Physics Platform | StormDash | core | `geometry` |
| 144 | `physics-blast` | Physics Blast | StormEduPlus | seed | `quiz` |
| 145 | `physics-pinball` | Physics Pinball | StormRetro | seed | `placeholder` |
| 146 | `pipe-escape` | Pipe Escape | StormMario | seed | `geometry` |
| 147 | `pixel-punch` | Pixel Punch | StormElite | seed | `placeholder` |
| 148 | `pixel-racer` | Pixel Racer | StormElite | seed | `geometry` |
| 149 | `pixel-warfare` | Pixel Warfare | StormElite | seed | `placeholder` |
| 150 | `platform-storm` | Platform Storm | StormNeon | core | `geometry` |
| 151 | `pong-duel` | Pong Duel | StormRetro | seed | `breakout` |
| 152 | `princess-rescue` | Princess Rescue | StormMario | seed | `geometry` |
| 153 | `probability_quest` | Probability Quest | StormPuzzle | core | `geometry` |
| 154 | `proof_builder` | Proof Builder | StormPuzzle | core | `geometry` |
| 155 | `punch-arena` | Punch Arena | StormElite | seed | `placeholder` |
| 156 | `python-pro` | Python Pro | StormNeon | core | `snake` |
| 157 | `qbert-jump` | Q*Bert Jump | StormRetro | seed | `maze` |
| 158 | `quick_sat` | Quick SAT | StormQuick | core | `geometry` |
| 159 | `ratio_architect` | Ratio Architect | StormPuzzle | core | `geometry` |
| 160 | `reaction-time` | Reaction Time | StormElite | seed | `quiz` |
| 161 | `reading-rush` | Reading Rush | StormEduPlus | seed | `typing` |
| 162 | `reflex-blitz` | Reflex Blitz | StormElite | seed | `quiz` |
| 163 | `retro-soccer` | Retro Soccer | StormElite | seed | `placeholder` |
| 164 | `revolution-x` | Revolution X | StormEduPlus | seed | `quiz` |
| 165 | `rhythm-storm-x` | Rhythm Storm X | StormElite | seed | `placeholder` |
| 166 | `robot-rumble` | Robot Rumble | StormElite | seed | `placeholder` |
| 167 | `robotron-2084-x` | Robotron 2084 X | StormElite | seed | `space` |
| 168 | `sat_word_arena` | SAT Word Arena | StormBattle | core | `typing` |
| 169 | `science_defender` | Science Defender | StormBattle | core | `quiz` |
| 170 | `science-lab` | Science Lab | StormEduPlus | seed | `quiz` |
| 171 | `science-quest` | Science Quest | StormNeon | core | `quiz` |
| 172 | `scramble-x` | Scramble X | StormElite | seed | `space` |
| 173 | `sentence_builder_pro` | Sentence Builder Pro | StormPuzzle | core | `geometry` |
| 174 | `sentence_sprint` | Sentence Sprint | StormDash | core | `geometry` |
| 175 | `sequence-storm` | Sequence Storm | StormRetro | seed | `memory` |
| 176 | `shadow-duel` | Shadow Duel | StormElite | seed | `placeholder` |
| 177 | `shell-shock` | Shell Shock | StormMario | seed | `geometry` |
| 178 | `skilldash` | SkillDash | StormDash | core | `geometry` |
| 179 | `sky-assault` | Sky Assault | StormElite | seed | `placeholder` |
| 180 | `sky-runner` | Sky Runner | StormNeon | core | `geometry` |
| 181 | `slam-dunk-x` | Slam Dunk X | StormElite | seed | `placeholder` |
| 182 | `snake-neon` | Snake Neon | StormNeon | core | `snake` |
| 183 | `snake-xl` | Snake XL | StormRetro | seed | `snake` |
| 184 | `space-invaders-x` | Space Invaders X | StormRetro | seed | `space` |
| 185 | `space-racer` | Space Racer | StormElite | seed | `geometry` |
| 186 | `space-raiders` | Space Raiders | StormNeon | core | `space` |
| 187 | `space-rock` | Space Rock | StormNeon | core | `asteroids` |
| 188 | `speed_multiplication` | Speed Multiplication | StormQuick | core | `geometry` |
| 189 | `speed_reading_dash` | Speed Reading Dash | StormDash | core | `typing` |
| 190 | `speed-typer` | Speed Typer | StormNeon | core | `typing` |
| 191 | `spell-storm` | Spell Storm | StormEduPlus | seed | `typing` |
| 192 | `spelling_sniper` | Spelling Sniper | StormQuick | core | `quiz` |
| 193 | `spelling-quest` | Spelling Quest | StormNeon | core | `scramble` |
| 194 | `stack-attack` | Stack Attack | StormNeon | core | `tetris` |
| 195 | `star-assault` | Star Assault | StormElite | seed | `space` |
| 196 | `star-power` | Star Power | StormMario | seed | `geometry` |
| 197 | `statistics_paintball` | Statistics Paintball | StormBattle | core | `geometry` |
| 198 | `storm_defenders` | Storm Defenders | StormBattle | core | `geometry` |
| 199 | `storm_defenders_vr` | Storm Defenders VR | Storm3D | core | `geometry` |
| 200 | `storm-fighter` | Storm Fighter | StormElite | seed | `placeholder` |
| 201 | `street-hoops` | Street Hoops | StormElite | seed | `placeholder` |
| 202 | `street-storm-fighter` | Street Storm Fighter | StormElite | seed | `placeholder` |
| 203 | `super-skillz-bros` | Super Skillz Bros | StormMario | seed | `geometry` |
| 204 | `tank-storm` | Tank Storm | StormElite | seed | `space` |
| 205 | `tempest-blast` | Tempest Blast | StormRetro | seed | `maze` |
| 206 | `tennis-storm` | Tennis Storm | StormElite | seed | `placeholder` |
| 207 | `tetris-neon` | Tetris Neon | StormNeon | core | `tetris` |
| 208 | `tetris-storm` | Tetris Storm | StormRetro | seed | `tetris` |
| 209 | `timeline_builder` | Timeline Builder | StormPuzzle | core | `geometry` |
| 210 | `timeline-rush` | Timeline Rush | StormEduPlus | seed | `quiz` |
| 211 | `times-table-titan` | Times Table Titan | StormNeon | core | `math` |
| 212 | `tower-blitz` | Tower Blitz | StormRetro | seed | `td` |
| 213 | `tower-defense-x` | Tower Defense X | StormElite | seed | `td` |
| 214 | `trivia-blitz` | Trivia Blitz | StormRetro | seed | `quiz` |
| 215 | `tron-cycles` | Tron Cycles | StormElite | seed | `maze` |
| 216 | `turbo-track` | Turbo Track | StormElite | seed | `geometry` |
| 217 | `type-speed-x` | Type Speed X | StormRetro | seed | `typing` |
| 218 | `typing-storm` | Typing Storm | StormNeon | core | `typing` |
| 219 | `vector-maze` | Vector Maze | StormRetro | seed | `maze` |
| 220 | `vocab-blast` | Vocab Blast | StormNeon | core | `quiz` |
| 221 | `vocab-blitz` | Vocab Blitz | StormEduPlus | seed | `typing` |
| 222 | `vocabulary_sniper` | Vocabulary Sniper | StormBattle | core | `quiz` |
| 223 | `vr_history_explorer` | VR History Explorer | StormVR | core | `quiz` |
| 224 | `vr_math_dojo` | VR Math Dojo | StormVR | core | `math` |
| 225 | `vr_science_lab` | VR Science Lab | StormVR | core | `quiz` |
| 226 | `word_balloon_pop` | Word Balloon Pop | StormQuick | core | `typing` |
| 227 | `word_connect_storm` | Word Connect Storm | StormPuzzle | core | `typing` |
| 228 | `word_rocket_run` | Word Rocket Run | StormDash | core | `typing` |
| 229 | `word_world_3d` | Word World 3D | Storm3D | core | `typing` |
| 230 | `word-blitz` | Word Blitz | StormNeon | core | `typing` |
| 231 | `word-scramble-x` | Word Scramble X | StormNeon | core | `scramble` |
| 232 | `word-storm` | Word Storm | StormRetro | seed | `scramble` |
| 233 | `word-wizard` | Word Wizard | StormEduPlus | seed | `typing` |
| 234 | `wordwave_survival` | WordWave Survival | StormBattle | core | `typing` |
| 235 | `world-8-rush` | World 8 Rush | StormMario | seed | `geometry` |
| 236 | `world-explorer` | World Explorer | StormNeon | core | `quiz` |
| 237 | `zaxxon-x` | Zaxxon X | StormElite | seed | `space` |

## Purpose stubs (for sprint assignment)

### algebra_blaster
- **Purpose:** Playable route /game/algebra_blaster — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### algebra-attack
- **Purpose:** Shoot the correct value of X to defeat waves of equation enemies!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### algebra-dash
- **Purpose:** Learn geometry concepts while navigating through shape-based levels!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### algo-quest
- **Purpose:** Build algorithms step-by-step to guide your robot through ever-more complex mazes!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### alien-blitz
- **Purpose:** Playable route /game/alien-blitz — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### anagram-storm
- **Purpose:** Playable route /game/anagram-storm — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### ancient-quest
- **Purpose:** Explore ancient civilizations — Egypt, Rome, Greece — in this historical adventure!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### arena-legends
- **Purpose:** Gladiatorial arena combat. 4 legendary warriors clash in the colosseum. Epic special moves, crowd roar effects, and championship rounds.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### arkanoid-x
- **Purpose:** Playable route /game/arkanoid-x — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### asteroid-storm
- **Purpose:** Playable route /game/asteroid-storm — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### asteroids-blast
- **Purpose:** Blast through asteroid fields in your spacecraft in this iconic space shooter!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### astromath_wars
- **Purpose:** Playable route /game/astromath_wars — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### atom-smash
- **Purpose:** Collide atoms to create elements — learn the periodic table through high-speed particle physics!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### battle-ships-x
- **Purpose:** Classic naval strategy reimagined! 8×8 grid, place shots with arrows, hunt enemy ships. CPU uses smart targeting AI to track your fleet down.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### battlezone-x
- **Purpose:** First-person tank combat in a vector-graphics battlefield!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### beat-blitz
- **Purpose:** High-tempo beat matching game. Synced note patterns, color-coded lanes, and max-combo chasing for top scores.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### binary-blast
- **Purpose:** Shoot down binary numbers to decode secret messages in this hacker-themed shooter!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### bio-burst
- **Purpose:** Navigate through human body systems, defeating viruses and learning biology along the way!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### block-storm
- **Purpose:** Playable route /game/block-storm — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### brain_boost
- **Purpose:** Playable route /game/brain_boost — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### brain-blitz
- **Purpose:** Playable route /game/brain-blitz — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### breakout-blitz
- **Purpose:** Smash through rows of bricks with your paddle and ball in this addictive classic!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### brick-crusher
- **Purpose:** Playable route /game/brick-crusher — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### brick-storm
- **Purpose:** Advanced brick-breaking with special bricks, power-ups, and boss stages!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### bull_run_logic
- **Purpose:** Playable route /game/bull_run_logic — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### bullet-hell-x
- **Purpose:** True danmaku bullet hell. Enemy bullet spirals, waves, aimed volleys. Tiny hitbox, upgradeable firepower, and screen-filling boss fights.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### burger-time-x
- **Purpose:** Build burgers by walking over ingredients while dodging angry hot dogs!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### capital-clash
- **Purpose:** Match capitals to countries faster than your rivals in this global trivia showdown!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### castle-siege
- **Purpose:** Storm the castle! Vertical platformer with challenging enemies and a massive boss fight.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### centipede-rush
- **Purpose:** Shoot the centipede as it winds down the screen in this frantic arcade shooter!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### chem_jump
- **Purpose:** Playable route /game/chem_jump — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### chem_lab_chaos
- **Purpose:** Playable route /game/chem_lab_chaos — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### chemistry-quest
- **Purpose:** Balance chemical equations and avoid dangerous reactions in this explosive puzzle game!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### chess-storm
- **Purpose:** Elite neon chess with full AI, animated piece movement, captured piece tracking, and check/checkmate detection. Use arrow keys to navigate.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### civics-storm
- **Purpose:** Master civics and government concepts in this fast-paced civic literacy challenge!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### code_breaker
- **Purpose:** Playable route /game/code_breaker — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### code-logic
- **Purpose:** Solve coding puzzles by arranging logic blocks — learn the fundamentals of programming!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### code-quest
- **Purpose:** Playable route /game/code-quest — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### coin-blitz
- **Purpose:** Race against the clock! Collect as many coins as possible before time runs out!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### color_equation
- **Purpose:** Playable route /game/color_equation — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### color-match
- **Purpose:** Match falling colored blocks as fast as possible in this addictive reflex game!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### color-rush
- **Purpose:** Guide the colored ball through matching colored gates at maximum speed!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### context_clue_hunt
- **Purpose:** Playable route /game/context_clue_hunt — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### coordinate_conquest
- **Purpose:** Playable route /game/coordinate_conquest — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### cosmic-blast
- **Purpose:** Playable route /game/cosmic-blast — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### crypt-runner
- **Purpose:** Fast-paced dungeon RPG. Sprint through crypts, collect power-ups, defeat undead enemies. Weapon selection: blade, crossbow, or arcane staff.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### crystal-castles-x
- **Purpose:** Collect gems across isometric crystal platforms while avoiding evil witches!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### cyber-highway
- **Purpose:** Infinite neon highway racer. Select your car, dodge AI traffic, hit turbos and race to the highest lap count!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### cyber-ninja
- **Purpose:** Fast-paced cyberpunk platformer. Flip gravity to navigate multi-surface levels, collect glowing orbs and dodge energy blade hazards.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### cyber-soccer
- **Purpose:** Neon soccer with puck-physics. Knock the ball into the goal, defend with WASD, fast AI challenge and satisfying goal explosions.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### cyber-sprint
- **Purpose:** Cyberpunk infinite runner — dodge obstacles and hack barriers at light speed!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### data_defender
- **Purpose:** Playable route /game/data_defender — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### debate_dash
- **Purpose:** Playable route /game/debate_dash — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### debug-dash
- **Purpose:** Find and fix bugs in code snippets at breakneck speed — the clock is ticking!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### defender-wave
- **Purpose:** Protect humans on the surface from alien abduction in this high-speed shooter!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### desert-rally
- **Purpose:** Rally racing through harsh desert terrain. Wide sweeping S-bends, gravel physics, and intense AI rivals on the orange circuit.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### dig-dug-x
- **Purpose:** Dig underground tunnels and inflate enemies to defeat them!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### donkey-run
- **Purpose:** Jump over barrels and climb ladders to rescue your friend from the gorilla!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### drift-king-x
- **Purpose:** Master the art of drift! Wide sweeping tracks designed for drifting. Car select with stat bars, drift physics, and glowing tire trails.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### dual-storm
- **Purpose:** Elite twin-stick arcade shooter. Auto-aim + WASD movement. Enemy waves escalate every round with new enemy types, power-ups, and shield pickups.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### dungeon-crawl
- **Purpose:** Roguelike dungeon explorer — fight monsters, find loot, survive!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### dungeon-crawler
- **Purpose:** Full top-down RPG! Choose sword, bow, or staff. Fight goblins, orcs, mages and bosses across procedurally generated dungeon floors. Level up, collect loot, upgrade stats.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### dungeon-run
- **Purpose:** Race through procedural dungeons. Twist, turn, collect score gems and escape each level before the clock runs out in this neon maze adventure.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### dungeon-storm
- **Purpose:** Elite dungeon RPG with sword/bow/staff combat, enemy AI, loot system, and 10+ procedural floors. Boss encounters scale with your level!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### equation_escape
- **Purpose:** Playable route /game/equation_escape — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### essay_builder_rush
- **Purpose:** Playable route /game/essay_builder_rush — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### financial_literacy_run
- **Purpose:** Playable route /game/financial_literacy_run — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### flag-frenzy
- **Purpose:** Identify country flags at lightning speed — over 195 flags to master!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### flappy-storm
- **Purpose:** Tap to flap through neon pipes in this addictive one-button classic!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### flash_fact_frenzy
- **Purpose:** Playable route /game/flash_fact_frenzy — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### fraction_frenzy
- **Purpose:** Playable route /game/fraction_frenzy — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### fraction-fighter
- **Purpose:** Playable route /game/fraction-fighter — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### fraction-frenzy
- **Purpose:** Match equivalent fractions before time runs out in this colorful fraction showdown!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### frogger-rush
- **Purpose:** Cross busy roads and rushing rivers to get your frog home safely!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### galaga-storm
- **Purpose:** Face endless waves of alien formations in this intense space shooter!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### galaxian-x
- **Purpose:** Shoot diving aliens in formation in this predecessor to Galaga!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### galaxy-storm
- **Purpose:** Playable route /game/galaxy-storm — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### geography-hero
- **Purpose:** Race the clock to place countries on the world map — become the ultimate geography champion!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### geography-quest
- **Purpose:** Playable route /game/geography-quest — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### geometry_defender
- **Purpose:** Playable route /game/geometry_defender — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### geometry_glide
- **Purpose:** Playable route /game/geometry_glide — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### geometry_runner_3d
- **Purpose:** Playable route /game/geometry_runner_3d — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### geometry-dash-storm
- **Purpose:** Jump, dodge spikes, and ride orbs through mind-bending neon levels!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### grammar_clicker
- **Purpose:** Playable route /game/grammar_clicker — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### grammar_gladiator
- **Purpose:** Playable route /game/grammar_gladiator — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### grammar-quest
- **Purpose:** Fix grammar errors across enchanted sentences to free the language kingdom!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### gravity-flip-x
- **Purpose:** Mind-bending gravity-flip platformer! Press S to flip gravity. Collect all coins, dodge hazards, and survive moving platforms across infinite levels.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### gravity-well
- **Purpose:** Navigate your ship using gravity wells — a unique physics-based space puzzle!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### grid-master
- **Purpose:** Playable route /game/grid-master — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### history_dash
- **Purpose:** Playable route /game/history_dash — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### history_timeline_rush
- **Purpose:** Playable route /game/history_timeline_rush — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### history-blast
- **Purpose:** Playable route /game/history-blast — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### history-blitz
- **Purpose:** Race through history — answer questions about key events to unlock new eras!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### horde-rush
- **Purpose:** Survive escalating waves of enemies. Dodge, auto-aim and blast everything. Shield pickups, health drops, wave bonuses. How long can you last?
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### ice-hockey-x
- **Purpose:** Full-field ice hockey arcade! Control your player with WASD, hit the puck, score goals. AI goalie adapts to your style. Fast and furious!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### joust-fighter
- **Purpose:** 2D fighting game with aerial jousting mechanics — lance your opponents!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### joust-storm
- **Purpose:** Mount your flying ostrich and joust against enemy knights to claim their eggs!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### kart-storm
- **Purpose:** High-speed top-down kart racing with power-ups, drifting, and competitive lap racing!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### logic_tower
- **Purpose:** Playable route /game/logic_tower — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### logic_tunnel
- **Purpose:** Playable route /game/logic_tunnel — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### logic-blast
- **Purpose:** Solve logic puzzles under time pressure — deduction meets arcade action!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### loop-storm
- **Purpose:** Master loops, conditions, and functions by controlling a storm of code blocks!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### lunar-lander-x
- **Purpose:** Carefully pilot your lunar module to a safe landing with limited fuel!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### map-master
- **Purpose:** Conquer continents by correctly identifying geographic features under time pressure!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### market_mayhem
- **Purpose:** Playable route /game/market_mayhem — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### math_galaxy_3d
- **Purpose:** Playable route /game/math_galaxy_3d — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### math-elite
- **Purpose:** Playable route /game/math-elite — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### math-quest
- **Purpose:** Solve math problems to defeat enemies and level up through magic kingdoms!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### math-storm
- **Purpose:** Playable route /game/math-storm — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### maze_of_ratios
- **Purpose:** Playable route /game/maze_of_ratios — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### maze-escape-x
- **Purpose:** Procedurally generated mazes every run! Navigate the neon labyrinth to the goal flag before time expires. Levels get bigger and harder.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### maze-runner-x
- **Purpose:** Find the exit through increasingly complex mazes before the timer runs out!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### memory_matrix
- **Purpose:** Playable route /game/memory_matrix — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### memory-flash
- **Purpose:** Watch the pattern, repeat it. How far can your memory take you?
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### memory-matrix
- **Purpose:** Playable route /game/memory-matrix — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### missile-command-x
- **Purpose:** Defend your cities from incoming missiles with your ABM battery!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### moon-patrol-x
- **Purpose:** Drive your buggy across the moon surface, shooting enemies and jumping craters!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### mr-do-x
- **Purpose:** Dig tunnels to collect cherries while throwing power balls at monsters!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### multiplication_meteors
- **Purpose:** Playable route /game/multiplication_meteors — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### mushroom-dash
- **Purpose:** Endless side-scrolling madness with mushroom power-ups and escalating obstacles!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### neon-breakout
- **Purpose:** Playable route /game/neon-breakout — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### neon-chess
- **Purpose:** Full chess with AI opponent using minimax algorithm! Glowing neon pieces, animated moves, legal move highlighting, check detection, and capture tracking.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### neon-circuit-racer
- **Purpose:** Top-down F1 racing with car selection! Choose VIPER, PHANTOM or CRUSHER. 3-lap neon city circuit with 3 AI opponents, speed meter, and live race rank.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### neon-dungeon
- **Purpose:** Neon-lit dungeon crawler RPG. Explore dark corridors, slay enemies, collect items, and descend through increasingly deadly floors.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### neon-fortress
- **Purpose:** Defend your neon base from waves of cyber enemies. Advanced tower placements, freeze effects, and rocket splash damage. No base gets through!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### neon-jump
- **Purpose:** Playable route /game/neon-jump — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### neon-jumper
- **Purpose:** Precision neon platformer. Ultra-tight jump timing, moving platforms, coin collection, gravity manipulation and escalating level design.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### neon-kombat
- **Purpose:** Neon-drenched arena combat. Select your warrior, master lightning combos, unleash devastating specials. Brutal 2.5D fighter with round system.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### neon-maze
- **Purpose:** Stunning glowing mazes with a countdown clock. Every maze is unique, generated fresh each run. How many floors can you escape?
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### neon-pong
- **Purpose:** Pong reimagined with neon glow effects, power-ups, and speed escalation!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### neon-racer
- **Purpose:** Tron-inspired neon racing through futuristic circuits!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### neon-viper
- **Purpose:** Playable route /game/neon-viper — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### neon-viper-xl
- **Purpose:** Playable route /game/neon-viper-xl — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### number_catch
- **Purpose:** Playable route /game/number_catch — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### number-battle
- **Purpose:** Playable route /game/number-battle — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### number-ninja
- **Purpose:** Slice the correct answer out of the air with ninja precision! Fast arithmetic action.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### number-storm
- **Purpose:** Math meets arcade — solve equations and shoot the correct answers!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### pac-maze
- **Purpose:** Navigate the maze, eat pellets, dodge ghosts in this timeless arcade legend!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### pattern_blast
- **Purpose:** Playable route /game/pattern_blast — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### pattern-match
- **Purpose:** A 6-round Wordle-style pattern matching game with retro visuals!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### pattern-storm
- **Purpose:** Playable route /game/pattern-storm — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### phoenix-x
- **Purpose:** Blast through five waves of alien birds in this Space Invaders cousin!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### physics_platform
- **Purpose:** Playable route /game/physics_platform — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### physics-blast
- **Purpose:** Solve physics puzzles using real-world forces, momentum, and gravity!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### physics-pinball
- **Purpose:** Full physics pinball! Realistic ball, flippers (A/D), neon bumpers worth 100-300pts each, multiplier system, and satisfying launch mechanism.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### pipe-escape
- **Purpose:** Navigate through a labyrinth of pipes. Think Pac-Man meets Mario in this maze classic!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### pixel-punch
- **Purpose:** 8-bit brawler with 4 characters, special moves, and a final boss!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### pixel-racer
- **Purpose:** Top-down pixel art racing with drifting mechanics and turbo boosts!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### pixel-warfare
- **Purpose:** 8-bit tactical combat — command pixel troops across battlefield maps!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### platform-storm
- **Purpose:** Playable route /game/platform-storm — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### pong-duel
- **Purpose:** The original 2-player paddle battle! First to 11 points wins!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### princess-rescue
- **Purpose:** Classic platformer adventure. Fight through 5 worlds to rescue the princess from the final boss!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### probability_quest
- **Purpose:** Playable route /game/probability_quest — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### proof_builder
- **Purpose:** Playable route /game/proof_builder — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### punch-arena
- **Purpose:** Boxing simulation with realistic physics and stamina management!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### python-pro
- **Purpose:** Playable route /game/python-pro — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### qbert-jump
- **Purpose:** Hop on cubes to change their color while avoiding enemies on this isometric puzzle board!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### quick_sat
- **Purpose:** Playable route /game/quick_sat — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### ratio_architect
- **Purpose:** Playable route /game/ratio_architect — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### reaction-time
- **Purpose:** The ultimate test — see how fast you can react to sudden color changes!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### reading-rush
- **Purpose:** Race through reading comprehension questions in this high-speed literary adventure!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### reflex-blitz
- **Purpose:** Hit the targets as they appear — the fastest reflex wins!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### retro-soccer
- **Purpose:** Classic top-down soccer with power shots and bicycle kicks!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### revolution-x
- **Purpose:** Re-live pivotal historical revolutions by answering questions and making key decisions!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### rhythm-storm-x
- **Purpose:** 4-lane neon rhythm game! Hit notes as they fall with A/S/D/F keys. PERFECT/GREAT/GOOD ratings, combo multiplier, and escalating BPM.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### robot-rumble
- **Purpose:** Mech battle arena! Choose your robot warrior, smash with hydraulic fists, deploy EMP specials. Steel-plated combat at its finest.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### robotron-2084-x
- **Purpose:** Last human vs robot uprising — shoot in all directions to survive!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### sat_word_arena
- **Purpose:** Playable route /game/sat_word_arena — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### science_defender
- **Purpose:** Playable route /game/science_defender — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### science-lab
- **Purpose:** Mix elements, run experiments, and discover the laws of physics in this virtual lab!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### science-quest
- **Purpose:** Playable route /game/science-quest — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### scramble-x
- **Purpose:** Horizontally-scrolling shooter — bomb fuel depots and fight waves of enemies!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### sentence_builder_pro
- **Purpose:** Playable route /game/sentence_builder_pro — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### sentence_sprint
- **Purpose:** Playable route /game/sentence_sprint — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### sequence-storm
- **Purpose:** Remember and repeat ever-growing sequences of lights and sounds!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### shadow-duel
- **Purpose:** Ninja vs ninja in total darkness. Swift strikes, smoke bomb specials, and lightning-fast combos. The fastest fighter in the arcade.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### shell-shock
- **Purpose:** Throw bouncing shells to eliminate all targets on each level. Timing is everything!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### skilldash
- **Purpose:** Playable route /game/skilldash — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### sky-assault
- **Purpose:** Vertical scrolling aerial combat — dogfight enemies and collect stars!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### sky-runner
- **Purpose:** Playable route /game/sky-runner — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### slam-dunk-x
- **Purpose:** Pixel basketball with trick shots, dunks, and overtime drama!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### snake-neon
- **Purpose:** Playable route /game/snake-neon — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### snake-xl
- **Purpose:** Grow your snake by eating, but don't hit the walls or your own tail!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### space-invaders-x
- **Purpose:** The classic alien shooter reborn with neon visuals and modern powerups!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### space-racer
- **Purpose:** Anti-gravity racing through the cosmos. Neon purple space circuit, zero-G handling, 3 selectable ships with unique handling stats.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### space-raiders
- **Purpose:** Playable route /game/space-raiders — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### space-rock
- **Purpose:** Playable route /game/space-rock — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### speed_multiplication
- **Purpose:** Playable route /game/speed_multiplication — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### speed_reading_dash
- **Purpose:** Playable route /game/speed_reading_dash — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### speed-typer
- **Purpose:** Playable route /game/speed-typer — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### spell-storm
- **Purpose:** Letters rain from the sky — arrange them into words before the storm hits!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### spelling_sniper
- **Purpose:** Playable route /game/spelling_sniper — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### spelling-quest
- **Purpose:** Playable route /game/spelling-quest — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### stack-attack
- **Purpose:** Playable route /game/stack-attack — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### star-assault
- **Purpose:** Wave-based space shooter with twin-stick controls. Alien formations, powered-up enemies and boss waves in deep space.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### star-power
- **Purpose:** Race through levels while a limited star power makes you invincible — use it wisely!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### statistics_paintball
- **Purpose:** Playable route /game/statistics_paintball — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### storm_defenders
- **Purpose:** Playable route /game/storm_defenders — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### storm_defenders_vr
- **Purpose:** Playable route /game/storm_defenders_vr — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### storm-fighter
- **Purpose:** Classic 2D street fighter with special moves and combos!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### street-hoops
- **Purpose:** Street basketball with real shot physics! Move, aim with arrow keys, charge your shot, release for a perfect arc. 2-pointer or 3-pointer scoring!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### street-storm-fighter
- **Purpose:** 4 selectable fighters! STORM, BLAZE, FROST or VOLT. Full health bars, special meter, punch/kick/special combos, AI rounds. Best-in-class 2D fighter!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### super-skillz-bros
- **Purpose:** Classic side-scrolling platformer. Run, jump, stomp enemies, collect coins across exciting levels!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### tank-storm
- **Purpose:** Command your tank on the battlefield! Rotate hull, aim turret independently, fire bouncing shells. Destructible walls, smart enemy tanks.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### tempest-blast
- **Purpose:** Shoot enemies crawling up the edges of geometric shapes in this vector classic!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### tennis-storm
- **Purpose:** Intense arcade tennis! Control your paddle with W/S, rally to build combos, and outlast the AI. Track your longest rally and total score.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### tetris-neon
- **Purpose:** Playable route /game/tetris-neon — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### tetris-storm
- **Purpose:** Stack falling blocks and clear lines in this legendary puzzle game!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### timeline_builder
- **Purpose:** Playable route /game/timeline_builder — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### timeline-rush
- **Purpose:** Place historical events in chronological order before time runs out!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### times-table-titan
- **Purpose:** Playable route /game/times-table-titan — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### tower-blitz
- **Purpose:** Defend your tower from waves of enemies with strategic placement!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### tower-defense-x
- **Purpose:** Strategic tower defense with 4 tower types: CANNON, SNIPER, FREEZE, ROCKET. Place with arrow keys, send waves, upgrade as enemies swarm!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### trivia-blitz
- **Purpose:** Fast-fire trivia questions across gaming, pop culture, and science categories!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### tron-cycles
- **Purpose:** Light cycle battle — leave walls and trap opponents before they trap you!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### turbo-track
- **Purpose:** Overhead racing with nitro boosts, oil slicks, and dynamic weather!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### type-speed-x
- **Purpose:** Type the falling words before they reach the bottom! A typing game with high score chase!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### typing-storm
- **Purpose:** Playable route /game/typing-storm — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### vector-maze
- **Purpose:** Navigate your vector ship through procedurally-generated maze corridors!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### vocab-blast
- **Purpose:** Playable route /game/vocab-blast — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### vocab-blitz
- **Purpose:** Speed through vocabulary flashcards — match definitions faster than your opponent!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### vocabulary_sniper
- **Purpose:** Playable route /game/vocabulary_sniper — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### vr_history_explorer
- **Purpose:** Playable route /game/vr_history_explorer — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### vr_math_dojo
- **Purpose:** Playable route /game/vr_math_dojo — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### vr_science_lab
- **Purpose:** Playable route /game/vr_science_lab — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### word_balloon_pop
- **Purpose:** Playable route /game/word_balloon_pop — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### word_connect_storm
- **Purpose:** Playable route /game/word_connect_storm — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### word_rocket_run
- **Purpose:** Playable route /game/word_rocket_run — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### word_world_3d
- **Purpose:** Playable route /game/word_world_3d — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### word-blitz
- **Purpose:** Playable route /game/word-blitz — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### word-scramble-x
- **Purpose:** Playable route /game/word-scramble-x — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### word-storm
- **Purpose:** Find as many words as possible from a set of scrambled letters before time runs out!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### word-wizard
- **Purpose:** Cast word spells by spelling correctly! Master vocabulary to defeat the word dragon.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### wordwave_survival
- **Purpose:** Playable route /game/wordwave_survival — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### world-8-rush
- **Purpose:** The ultimate speed run challenge. Navigate the hardest levels ever designed at max speed!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### world-explorer
- **Purpose:** Playable route /game/world-explorer — core catalog title.
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.

### zaxxon-x
- **Purpose:** Isometric space shooter — navigate through fortress walls and shoot everything!
- **Engine contract:** `init/update/draw/getScore/isGameOver` via `NeonEngineInstance` (see `web/src/games/neon/types.ts`).
- **Distinct UX:** Theming via `slugTheme` + `resolveNeonTuning`; per-title HUD/overlays TBD in sprint.


---

_Regenerate: `npm run catalog:inventory --prefix web`_
